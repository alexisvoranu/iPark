apiVersion: v1
kind: Service
metadata:
  name: ipark-service
  namespace: default
  labels:
    app: ipark-backend
spec:
  type: LoadBalancer
  selector:
    app: ipark-backend
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080