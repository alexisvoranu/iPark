apiVersion: v1
kind: Service
metadata:
  name: ipark-service
  namespace: default
  labels:
    app: ipark-app
spec:
  type: NodePort
  selector:
    app: ipark-app
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080